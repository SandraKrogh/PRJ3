

int main()
{
    /*
    Opretter objekt af klassen PSoCcom
    Opretter objekt af klassen serial 
    Opretter objekt af klassen RpiData 

    kalder initialisering til PSoC under PSoCcom
        Bruger serial klassen 
        Send start signal til PSoC, som gør at den roterer til start 
    
    kommunikere med server 

    while(1)
    {
        case 1 
        {
            indstil spil
            gemmer data i RpiData(fil)
        }

        case 2
        {
            indstil antal spiller
            gemmer data i RpiData(fil)
        }

        case 3
        {
            Start spil
            kalder funktion startSpil, sender data object med

            while(ack ikke modtaget)
            {
                venter på svar fra PSoC
                if fejl
                    web fejl
                else
                    web succes
            }
        }

        case 4 
        {
            Kan håndtere fejlbesked
            evt. som interrupt 
        }
    }
    */
}

